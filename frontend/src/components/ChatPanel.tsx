import {useState} from 'react';import {sendChat} from '../api/client'
export function ChatPanel({candidate,batch}:{candidate:string;batch:string|null}){const[q,setQ]=useState('');const[history,setHistory]=useState<Array<{q:string;a:string;data:Record<string,unknown>}>>([]);const[busy,setBusy]=useState(false);async function ask(){if(!q.trim())return;setBusy(true);try{const r=await sendChat(candidate,q,batch);setHistory(h=>[...h,{q,a:r.answer,data:r.supporting_data}]);setQ('')}finally{setBusy(false)}}return <section className="card chat"><div className="section-head"><div><span className="step">05</span><h2>Ask grounded questions</h2></div><span className="pill">Scope: {batch?'current batch':'all history'}</span></div>{history.map((h,i)=><article key={i}><p className="question">{h.q}</p><p>{h.a}</p><details><summary>Supporting data</summary><pre>{JSON.stringify(h.data,null,2)}</pre></details></article>)}<div className="chatbox"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&ask()} placeholder="How many RFPs were routed?"/><button disabled={busy} onClick={ask}>{busy?'Checking…':'Ask'}</button></div></section>}
