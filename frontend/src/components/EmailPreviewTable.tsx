import type {Email} from '../types'
export function EmailPreviewTable({emails}:{emails:Email[]}){if(!emails.length)return null;return <section className="card"><div className="section-head"><div><span className="step">02</span><h2>Raw preview</h2></div><span className="pill">{emails.length} emails · nothing routed yet</span></div><div className="table-wrap"><table><thead><tr><th>Sender</th><th>Subject</th><th>Received</th><th>Thread</th><th>Body preview</th></tr></thead><tbody>{emails.map(e=><tr key={e.email_id}><td><strong>{e.from_name}</strong><small>{e.from_email}</small></td><td>{e.subject||<em>Empty subject</em>}</td><td>{new Date(e.received_at).toLocaleString()}</td><td><code>{e.thread_id}</code></td><td><details><summary>{e.body.slice(0,100)}{e.body.length>100?'…':''}</summary><pre>{e.body}</pre></details></td></tr>)}</tbody></table></div></section>}
