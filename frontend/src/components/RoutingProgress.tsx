export function RoutingProgress({done,total,routing,ready,onRoute}:{done:number;total:number;routing:boolean;ready:boolean;onRoute:()=>void}){return <section className="routebar"><div><span className="step">03</span><strong>Route with evidence</strong><small>{routing?`Completed ${done} of ${total} chunks`:ready?'Requests run sequentially and every result is persisted before confirmation.':'Waking backend—routing will unlock when readiness passes.'}</small></div><button disabled={routing||!ready} onClick={onRoute}>{routing?'Routing…':ready?'Route emails':'Waking backend…'}</button></section>}
