import {render,screen,fireEvent} from '@testing-library/react';import {vi,test,expect} from 'vitest';import App from '../src/App'
test('invalid JSON never invokes ingest and preview precedes routing',async()=>{const fetchMock=vi.fn(async(input:RequestInfo|URL)=>{const url=String(input);if(url.endsWith('/api/config'))return new Response(JSON.stringify({app_name:'Alumnx AI Sales inbox task router',candidate_id:'mahendrushivam123@gmail.com',max_ingest_emails:100}),{status:200});if(url.endsWith('/ready'))return new Response(JSON.stringify({status:'ready'}),{status:200});return new Response('{}',{status:500})});vi.stubGlobal('fetch',fetchMock);render(<App/>);const editor=screen.getByLabelText('Email JSON');fireEvent.change(editor,{target:{value:'{'}});fireEvent.click(screen.getByText('Preview JSON'));expect(await screen.findByRole('alert')).toHaveTextContent('Could not complete');expect(fetchMock.mock.calls.some(c=>String(c[0]).endsWith('/ingest'))).toBe(false)})
